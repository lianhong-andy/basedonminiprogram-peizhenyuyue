<van-card wx:for="{{serverList}}" class="padding" bindtap="todetail" data-id="{{item._id}}"
  price="{{item.price}}"
  desc="{{item.phone}}"
	tag="{{item.sworkplacename}}"
  title="{{item.name}}"
  thumb="{{ item.image }}"
/>